{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "esbuild app.js --bundle --format=iife --outfile=dist/bundle.js --platform=browser",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@tauri-apps/api": "^2.11.0",
    "esbuild": "^0.28.0"
  }
}
